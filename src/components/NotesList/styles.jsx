import styled from 'styled-components'

export const NotesWrap = styled.main`
    grid-area: main;
    padding: 1.5rem;
`

export const NotesList = styled.ul`
    display: flex;
    flex-wrap: wrap;
    gap: 2rem;
`